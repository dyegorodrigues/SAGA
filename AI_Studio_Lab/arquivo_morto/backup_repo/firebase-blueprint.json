{
  "entities": {
    "userState": {
      "title": "User State",
      "description": "Durable cloud-persisted state containing profiles, progress, wallets, albums, and logs of children.",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "Unique user or device ID." },
        "state": { "type": "string", "description": "JSON string of the full progress and analytics State object." },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "state", "updatedAt"]
    }
  },
  "firestore": {
    "/userStates/{userId}": {
      "schema": "userState",
      "description": "Durable persistent document for a specific device or user's whole progress and analytics."
    }
  }
}
