import React from "react";
import { Question } from "../../types";
import { C } from "../Mascot";

export function LegacyExercise({ 
  q, 
  status, 
  guidedIdx, 
  playAulinha,
  handlePick,
  pickedId, // previously `sel`
}: any) {
  // We'll migrate the big switch block here
  return <div>MIGRATING</div>;
}
