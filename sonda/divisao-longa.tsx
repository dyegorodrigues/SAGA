import React from "react";import{createRoot}from"react-dom/client";import"../src/index.css";import{FichaRenderer}from"../src/components/FichaRenderer";import{shouldRenderQuestionOptions}from"../src/components/gameloop/answerPolicy";import{generateRegisteredFichaQuestion}from"../src/curriculum/motores/composerCanary";import type{DivisaoLongaF69Spec}from"../src/curriculum/procedimentos/divisaoLongaContract";import type{AnswerMeta}from"../src/types";
const level=Math.max(1,Math.min(5,Number(new URLSearchParams(location.search).get("level")||1))),question=generateRegisteredFichaQuestion("N4.10",level),spec=question.uiProps as DivisaoLongaF69Spec;type R={value:string;correct:boolean;meta?:AnswerMeta};function Probe(){const[receipts,setReceipts]=React.useState<R[]>([]);return <main className="min-h-screen bg-slate-50 px-1 py-4"><section className="mx-auto max-w-4xl rounded-3xl bg-white px-2 py-4" data-f69-probe data-level={level} data-mode={spec.modo} data-answer={spec.resposta} data-generic={shouldRenderQuestionOptions(question)?"true":"false"} data-steps={question.resolucao?.passos.length??0} data-final={String(question.resolucao?.passos.at(-1)?.parcial??"")} data-evidence={question.exigeEvidencia??""} data-receipts={JSON.stringify(receipts)}><p className="text-center font-black">{question.prompt}</p><FichaRenderer question={question} onAnswer={(v,c,m)=>setReceipts(r=>[...r,{value:String(v),correct:c,meta:m}])}/></section></main>}createRoot(document.getElementById("root")!).render(<Probe/>);
